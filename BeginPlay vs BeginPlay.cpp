 정리해보자. 결국 클로드의 말이 맞았다. 언젠가 스콧아저씨의 책에서 읽었듯이 변수 혹은 함수의 호출 순서가 랜덤이기에 절대 알 수 없는 경우가 있는데
지금의 BeginPlay()들이 그 경우이다. 즉, 캐릭터의 BeginPlay()가 호출되었을 때 아직 컨트롤러의 BeginPlay()는 호출되지 않았고, 이는 컨트롤러의 
내부에 모든 것들이 완전히 준비가 되지 않았음을 뜻하며 이것이 위젯일 수가 있다. 따라서 초기화를 캐릭터의 BeginPlay()가 아니라 컨트롤러의 
BeginPlay()로 바꾸었다. 구현 형태는 컨트롤러가 GameInstanceSubsystem에서 스탯 초기화값을 가져와 캐릭터에도 넘기고 자기 자신이 UI를 초기화하는
데에도 쓰는 방식이다. 캐릭터를 불러오는 GetCharacter()는 이미 컨트롤러가 BeginPlay()를 호출했기에 그 이전에 Possess를 완료한 것이고 함수는 
만들어지는게 아니고 런타임때에 존재하기에 문제될 것이 없다. 이처럼 어떤 포인터 객체에 접근한다면 그것이 존재하는 클래스의 BeginPlay()에서 
구현해야 할 듯 하다.

1. 컨트롤러, 캐릭터 둘중 어느 쪽이라도 BeginPlay()가 호출된다면 이미 둘의 Possess가 완료된 시점이므로 서로의 포인터를 불러오는데에 실패는 없다.
2. BeginPlay()의 순서는 절대 알 수 없다.
3. 함수는 코드 컴파일 이후 런타임 시점에 생성된다.
4. GameInstanceSubsystem과 같은 언리얼의 체제 시스템은 항상 우선순위에 놓여있기에 액터등 보다 훨씬 더 빨리 생성된다.
5. 이번에 스탯값 같은 단순한 수치값들은 이미 생성자에서 그 초기화가 끝나지만 포인터 객체들은 그렇지 않기에 주의할 것.